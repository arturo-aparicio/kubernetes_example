# Default values for docker-app.
# This is a YAML-formatted file.
# Declare name/value pairs to be passed into your templates.

replicaCount: 1
restartPolicy: Never

image:
  repository: gke.jfrog.team/docker-app
  tag: latest
  secretName: regsecret
  pullPolicy: Always

#Uncomment if you want to generate docker credentials using helm
#imageCredentials:
#  registry: docker.artifactory
#  username: admin
#  password: password

service:
  annotations: {}
  clusterIP: ""
  externalIPs: []
  loadBalancerIP: "10.0.10.12"
  loadBalancerSourceRanges: []
  type: LoadBalancer
  port: 8181
  nodePort: ""

resources: {}

nodeSelector: {}
